#include "hl.h"
#include <stdio.h>

int main()
{
	int a = 10;
	int b = 2;

	int c = _add(a, b);
	int d = sub(a, b);
	int e = mul(a, b);
	int f = div(a, b);
	int g = mod(a, b);

	printf("%d", c);
//	printf("%d %i %d %d %d", c,d,e,f,g);

	return (0);
}
