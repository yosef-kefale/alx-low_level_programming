#include "holberton.h"
/**
* print_sign - does my stuff
* @n: its mine damnit
*
* Return: sign of number
*/
int print_sign(int n)
{
if(n < 0)
  return (-1);
if(n == 0)
  return (0);
if(n > 0)
  return (1);
}
